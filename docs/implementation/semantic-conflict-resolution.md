# Explicit semantic conflict resolution

The operator answer is coordination evidence, not approval or verified venue truth. `admin.resolveSemanticConflict` closes one exact lower-authority conflict with an explicit human decision. It records hashes and references to the retained answer rather than copying a hidden transcript.

## State transitions

- The original targeted proposal must still be APPROVED and reproduce the exact conflict preview. The question must be ANSWERED with the exact operation, timestamps, answer hash and callback binding.
- KEEP_CANONICAL records the decision and retires the original proposal as REJECTED. It creates no replacement.
- PROPOSE_REPLACEMENT records the original conflicting desired state separately from explicitly edited replacement content. It retires the original and creates one HUMAN-authored PENDING_REVIEW proposal. Original support lineage remains through the immutable resolution-to-original-proposal relation; duplicating the original support-version unique key is prohibited.
- A replacement remains blocked before its separate human review. Once approved, the preview service can honor the explicit adjudication only for the exact relation, replacement desired state, answered evidence and unchanged canonical snapshot. The source authority label remains TRUSTED_PARTNER; this is an operator decision, not a new claim of venue verification.
- Existing package/universal-content draft, adoption, approval and publication gates still apply. The resolution action performs no canonical mutation, scheduling or publication.

## Concurrency and scope

The service takes the existing tenant/venue content lock, then the original proposal row lock, answered question share lock and target share lock. It recomputes the preview after obtaining the row locks. Resolution IDs and request hashes bind exact retries; the unique question binding prevents a second decision. All row reads have explicit tenant/venue filters. Foreign operation collisions fail closed and roll back the attempted replacement.

The immutable SQL record has composite foreign keys, a trigger requiring the exact approved original, answered semantic question and unapproved human replacement, plus append-only enforcement in PostgreSQL and tenant middleware. The preview service independently checks the stored canonical hash and current question evidence; drift requires another explicit conflict resolution. An old successful retry returns its immutable result and never reactivates old content.

The final consumer also pins the replacement proposal target, proposed text, HUMAN creator type and creator ID to the immutable resolution. An edited or re-authored replacement cannot reuse the prior decision. Existing proposals without a resolution retain their previous preview hashes.

## Operator review

`SemanticUpdatePreview` exposes resolution only for an approved, non-temporal targeted correction/supersession with exactly one LOWER_AUTHORITY_CONFLICT blocker and its answered question. The scoped preview router returns the exact server-computed answer hash. The form shows the answer, requires an explicit keep/replace choice and note, and allows replacement wording to be edited independently of the original conflicting desired state.

Before saving, the form freezes the operation UUID and complete request. The parent disables the original preview fields and Close while the request or an unknown outcome is retained. A 15-second bounded request exposes an explicit exact retry on unknown outcomes. Known conflict/not-found/precondition errors require a fresh canonical preview; a new preview generation resets the decision even if the returned snapshot is unchanged. Scope changes abort and fence late results. Retry state is scoped to the mounted review, not advertised as browser-restart persistence.

Success replaces the original proposal controls with the recorded outcome and a scoped review link. The surrounding review row immediately shows CLOSED AFTER RESOLUTION for the exact tenant, venue, proposal and version; markers are pruned to the displayed review set and cannot relabel a newer version. A replacement opens a fresh proposal-review page at its row anchor and still requires separate approval. The form does not create drafts, approve, publish, or mutate canonical guidance.

## Proof and remaining gates

Native `2a3918f077fd` passes on 243 migrations / 261 tables with 29 stable source hashes; PostgreSQL stopped. Its measured source manifest describes the retained historical backend candidate; subsequent adapter and projection changes have separate proof below. See `docs/evidence/semantic-conflict-resolution-native-2026-09-10.json` for exact revision and proof limits. The UI has a separate isolated browser fixture; its API responses are mocked and cannot establish a browser-to-database or live-provider claim. Existing provider/device/customer/release gates and all original requirements remain intact. This slice does not close the full six-class updater, all 32 QA cases, or A/B/C acceptance.

The operator UI proof is retained in `docs/evidence/semantic-conflict-resolution-ui-2026-09-10.json`: candidate `b81f8677`, four viewport cases, 13 stable source hashes, 46 targeted tests, and the owned Next server stopped. The first ambiguous browser selector failure and intermediate passing runs remain retained with their narrower source limits.

## Connected review and saved wording

Candidate `3cfc4220` projects the exact immutable replacement desired state and relation in the scoped proposal list. The preview restores and locks those values; switching scope clears them. The dedicated connected fixture is development-only and requires its explicit integration flag.

`docs/evidence/semantic-conflict-resolution-connected-2026-09-10.json` retains native/browser proof `022796c42f00`: 243 migrations, 261 tables, 46 stable source hashes, one passing connected case, and PostgreSQL/Next stopped. It observes the committed resolution before the lost-response retry, recovers exactly one replacement, separately approves through the real review component, and renders its saved-wording correction preview at 1280/390. Canonical content and module/publication counts remain unchanged. Authentication is synthetic; fixture reload is not a claim about production document routing.

Private legacy adoption now connects a CORRECTION/SUPERSESSION whose target has no native module to the existing adoption service, exact snapshot checks, and bounded provenance reconstructed through the immutable resolution's original support proposal. The package-patch button remains a separate path. Do not copy the unique original support request/version key onto the replacement, bypass proposal binding with the general workbench, or infer publication permission.

## Private legacy adoption adapter

The admin preparation query returns exact proposal/preview/legacy snapshot preconditions from the existing semantic preview and canonical snapshot reader. Creation remains a separate admin mutation through the existing adoption service. The human actor comes from the authenticated session. The new adapter checks that the typed payload carries the same title and content as the approved preview; relationship payloads cannot represent this legacy text and are rejected by this adapter.

Preparation is read-only and is not authorization to publish. Creating the private v1 adoption draft can carry the resolved correction itself. Do not automatically append another semantic universal-content revision: that path requires an exact latest published native target, which a private adoption draft does not provide. The old legacy source remains authoritative until separate publication.

The support UI derives bounded source evidence through the immutable resolution-to-original-proposal relation. Do not copy the original support-version unique key onto its replacement. The low-level admin adapter accepts explicitly authored evidence; the support UI exclusively uses the stricter support-specific mutation described below. Publication creates a separate activation record and leaves the adopted legacy row immutable. The native regression checks exact service replay after publication; that is not browser-restart recovery proof. The existing SQL receipt guard already rejects native-linked targets; the added transactional service check provides an earlier domain conflict.

The support-specific `createSupportLegacyKnowledgeAdoptionDraft` mutation now rejects caller-supplied evidence and reconstructs it from frozen, scoped support-message lineage. Its resolver follows the immutable resolution link to the original proposal, validates the retained request-version audit event, and preserves the bounded ordered message IDs. It returns source references and hashes, never message bodies. The original proposal may be REJECTED after resolution; the current replacement still requires separate APPROVED status and exact semantic checks at creation. The operator panel uses this source-bound mutation.

The adapter rejects disabled desired guidance because native typed draft payloads do not encode the legacy enabled flag. Disabling guidance requires its separate retirement path; adoption must not silently discard that state.

## Operator private draft controls

Candidate `64cfef99` exposes the private draft controls only for supported approved, non-temporal correction/supersession previews with support provenance and an unlinked legacy target. The list exposes only eligibility booleans, not nested source records. Preparation pins the preview hash; content type has no default; approved title/wording remain fixed. The operator supplies required kind-specific metadata and sees the audience explicitly. Disabled guidance uses its separate retirement path.

Before creation the complete request freezes. A bounded request exposes exact retry after an unknown outcome; known rejection requires preparation again. Scope changes fence late results; the parent freezes Close, wording and recomputation while the outcome is retained. Success links to the existing scoped content review, with no publication action in this form. Browser-restart recovery remains outside this component's mounted-session contract.

Connected proof `cd5b3dcac439` on candidate `3c006c5e` passes one native/browser case across prepared and success states at390/768/1280/1440, with54stable source hashes,243migrations/261tables and both servers stopped. It observes the committed private draft before exact retry, verifies retained original support evidence, and leaves publication/activation at zero. See `docs/evidence/support-adoption-connected-browser-2026-09-10.json`. Initial run081d477c6f62 caught receipt contrast and is retained as failed. Native-target/addition support drafting was the next link after that historical candidate; the connected controls are described below.

## Source-bound universal drafts and adoption ownership

The support-specific universal draft adapter derives evidence from the same bounded frozen support lineage as the legacy adoption adapter. It preserves the approved wording and uses the existing exact native base checks. Addition and native supersession have real disposable-router proof in `docs/evidence/support-universal-source-native-2026-09-10.json`; their operator controls are described below.

An adoption already contains its proposal's corrected wording. That proposal must continue to reference its existing adoption revision, including after publication; it must never append a second revision through the support universal route. The adapter checks the scoped receipt early for feedback and repeats the check inside the existing locked draft transaction. The internal optional precondition is not a new client or MCP input. Publication remains a separate explicit action.

The operator UI consumes the server-derived target authoring state query described below. Raw legacy native-link columns stay null after adoption activation and are insufficient for routing. Own adoption receipt always takes precedence and links to the existing revision/status. An unactivated adoption belonging to another proposal blocks new draft creation. An activated adoption belonging to another proposal or a direct native target must satisfy the existing latest published revision checks and fixes the content kind. Untargeted additions and genuinely unadopted legacy targets remain separate choices. Partial links, withdrawn publications, or newer unpublished revisions must fail closed. Reuse the current form and canonical services rather than inventing browser base-version checks.

The later combined connected browser run `b8e9d421a11f` measured candidate `763f2624`, with 54 stable source hashes, eight rendered states across four widths, one passing case, and both servers stopped. Its retained manifest predates the subsequent universal-route transaction guard, which has separate native proof.

## Authoring state query

`getSupportProposalAuthoringState` is a read-only admin query pinned to tenant, venue, proposal and expected proposal version. It returns the existing receipt first, even when the proposal target changed, then distinguishes untargeted, unadopted legacy, another proposal's private adoption, ready native and unavailable/stale native targets. An own adoption remains an own receipt after publication or withdrawal. Current module publication and revision snapshots must not be confused with the original activation event.

This query is display state, not approval, a prepared draft or publication permission. The existing mutations still perform their exact semantic preview, provenance, receipt and locked version checks. The form consumes this state during explicit preparation; the existing `hasLegacyTarget` list boolean is not upgraded into authoring authority.

Native proof `05b47004bfca` (37 measured sources) and `02d63351d104` (28 measured sources) pass on candidate `16268a71`, with PostgreSQL stopped after each. See `docs/evidence/support-authoring-state-native-2026-09-10.json`. The two real-router cases preserve prior adoption/race and addition/supersession assertions while adding authoring-state reads; 21 targeted unit tests and API type/lint checks pass.

## Connected support authoring controls

The existing private-draft form handles support additions and native corrections/supersessions as well as legacy adoption. Explicit preparation reads scoped authoring state. Existing receipts show the original revision and separately qualified current module publication state; the historical activation never substitutes for current status. Another private adoption, missing/partial/conflicting links and stale publications lead to an explanation and scoped content-review link. Native content kind is fixed, while a new addition requires explicit kind selection.

A support-sourced approved non-temporal ADDITION/CORRECTION/SUPERSESSION uses this typed content route even when the semantic preview also has a legacy venue-package patch. ADDITION always has such a patch, so excluding it would hide the intended route. The older package-draft button remains available for other proposals. Mutation approval, exact preview, source evidence, locked receipt and publication checks are unchanged.

Unknown creation outcomes retain the entire exact request including its route. Stale preparation responses cannot cross proposal scopes. Recovered receipts do not invent a version or describe a newer module publication as publication of this draft. Row-level review panels are labeled groups so several open proposals do not create duplicate screen-reader landmarks.

Connected proof `377a67194d49` passes at candidate `97007cf7`:55 stable source hashes,24 rendered states at390/768/1280/1440, one passing case, and both servers stopped. It preserves the legacy flow and proves addition/retry, explicit fixture publication, native supersession and receipt re-entry. See `docs/evidence/support-authoring-connected-browser-2026-09-10.json` for source identities, failed runs, screenshots and exact limits.

## Support completion observes source-bound content

Candidate `694081e6` upgrades newly prepared fulfillment evidence to contract version 3. Existing package APPLY and guest-observability checks remain intact. The shared completion reader additionally follows exact support proposals and one immutable conflict-replacement hop, validates the frozen request audit version, and reads scoped universal/adoption receipts. Proposal and receipt reads are bounded at 100 and fail closed above that bound. The audit lookup preserves both the compound request-version identity and top-level tenant/venue fields required by tenant middleware.

Every content receipt must name the module's latest revision and latest PUBLISH event, be public and currently effective, and have the exact enabled public projection. The actual guest-read adapter must return the same stable projected fields; matching an ID alone is insufficient. Evidence freezes source proposal/request version, receipt, module, revision, publication, projection, observed content hash and guest route/release/state. Verification timestamps do not change the approval digest. A changed receipt or observed content does; the native state hash intentionally also requires approval refresh after unrelated native state changes.

A source proposal with no receipt cannot masquerade as no content work. Only terminal REJECTED proposals or an exact package handoff already verified by the package layer are exempt. Temporal handoffs without their own completion evidence remain blocked. Existing v1 approvals remain compatible only for requests with no packages and no typed receipts; v2 approvals require refresh into current evidence. Human completion re-reads the shared gate; agent completion additionally compares the reviewed immutable evidence under the existing venue content lock. No publication or delivery authority was added.

Native proof `cc03e2e7d387` passes two cases: source-bound addition/supersession and the existing reviewed in-app completion/retry lifecycle. It retains 38 manifest entries for 37 unique source paths, 243 migrations/261 tables and stopped PostgreSQL. Missing-receipt and private-draft reads reject; explicit fixture publication and observable projection permit exact current receipt evidence. A first failed run exposed the missing top-level audit query scope; the fix uses no isolation bypass. See `docs/evidence/support-content-completion-native-2026-09-10.json`.

At the version 4 checkpoint, remaining completion lanes included terminal no-op outcome presentation and same-request historical receipt supersession. Version 5 implements the latter below. Broader exact-candidate release/physical field QA remains open. Do not silently discard receipts, infer completion from proposal APPROVED, or publish automatically to satisfy this gate. This slice does not complete all six semantic outcome classes or the original campaign acceptance suite.

Resolved-adoption proof `0db4d14dc666` also passes on candidate `6db637bb`, with 42 measured entries (42 unique sources), one combined case and PostgreSQL stopped. It binds the replacement adoption receipt to the original frozen support request and the actual publication-created projection. The fixture first incorrectly expected the legacy entry ID; the corrected test queries the exact scoped published projection and retains the existing duplicate-route race checks.

## Temporal completion evidence

Candidate `14879699` implements temporal completion through fulfillment contract version 4. The shared bounded source resolver now serves both typed and temporal content; each direct or one-hop replacement endpoint still requires its own exact frozen support audit event. The temporal reader follows immutable operational handoffs without creating or scheduling anything.

Only a published, active update with retained publication time, startsAt <= verification time < expiresAt, and an exact public place scope can count. It must occur in the same ordered top-20 public operational-update read used by chat. The receipt freezes original proposal/request version, handoff/update identity, publication/update timestamps, window, and a hash of actual guest fields including place name. The typed reader exempts a temporal proposal only after this reader verified that exact proposal ID. Rejected source status never makes an existing temporal handoff disappear from verification.

All completion readers acquire request -> venue -> sorted operational entity locks. The extracted request primitive retains its existing advisory key. This order covers manual completion, proposal preparation and MCP's initial read without an inverted lock cycle. Operational writers retain their existing entity -> capacity order. The reader validates that the final handoff identity set exactly matches the set it locked, then uses a post-lock clock. Natural expiry is checked again after attachment resolution immediately before the completion status CAS. Verification-time changes alone do not change the approval digest; changed content, place identity, update timestamp, publication or window does. Version 2/3 approvals require refresh into version 4 evidence.

Native proof `6f8c1bcc29b6` passes temporal lifecycle and locked expiry plus the existing completion replay case on candidate `2fbe88bb` (45 sources). It observes the exact reader backend waiting on the operational advisory lock, crosses expiry using PostgreSQL's clock, releases the writer, and verifies rejection. Addition/supersession plus completion replay also pass in `d7e11ee89f1f` (42 sources, candidate `14879699`); resolved adoption passes `d9cdc5e11a2a` (47 sources, candidate `2fbe88bb`). At that checkpoint the measured production sources matched the retained manifests and all PostgreSQL instances stopped. These manifests are historical evidence for those revisions; version 5 changes several measured sources. These are four distinct cases across five executions, not the full campaign QA suite. See `docs/evidence/support-temporal-completion-native-2026-09-10.json`.

## Same-request receipt supersession

Candidate `fbd5c9e3` implements fulfillment version 5 with content evidence version 2. The chain resolver retains every immutable receipt, grouped by request, module and kind. Each historical receipt explicitly names its successor; only the single terminal carries current publication, projection and guest observation. Historical observation fields are null, rather than implying the old revision is still published.

A successor must pair CORRECTION/CORRECTS or SUPERSESSION/SUPERSEDES and name the predecessor's exact revision ID/version with a consecutive revision version. Each endpoint independently retains its frozen support request version. Ordinary continuations require a newer request version; equal versions require an explicit immutable conflict replacement link to the predecessor proposal. Adoption can be a root. A first correction may start against a base outside this request, but subsequent local links must be exact. Singleton receipts receive the same semantic/base validation as chains. Duplicate identities, forks, detached nodes, mixed kinds, invalid bases and backward source order fail closed.

Only the terminal must match current public/effective content and the actual guest projection. An unrelated later revision still blocks the original request. Current typed receipt start/end/operational expiry bounds are rechecked immediately before completion CAS; expired superseded history does not invalidate a current terminal. Timestamps alone do not invalidate approval equality. Earlier version 2-4 approvals require refreshed evidence; no-content version 1 compatibility remains narrowly scoped.

Three native runs pass against `fbd5c9e3`: `e30b4786cfb5` (43 sources), `12d83352567b` (48), and `8fd06acace43` (46). All measured bytes match at this checkpoint and PostgreSQL stopped after each. Four distinct cases execute five times, including repeated completion lifecycle regression. The addition fixture appends a real later message to the same request, proves private supersession blocks, explicitly publishes the fixture revision, verifies both receipt states, and rejects the old request after an unrelated later revision. There are also 116 targeted passing tests and DB/API/contracts type/lint checks. See `docs/evidence/support-receipt-chain-native-2026-09-10.json`. Adoption-root multi-receipt and equal-version edges currently have unit proof only; native adoption regression covers a single current replacement receipt.

### Next implementation: durable no-change outcomes

DUPLICATE_NOOP is currently a semantic preview outcome without its own durable fulfillment action or receipt. Bare REJECTED status is not proof of why no change was needed. The existing immutable KEEP_CANONICAL conflict resolution is distinct and requires an answered lower-authority conflict; do not repurpose it as automatic duplicate evidence.

Implement an explicit reviewed, scoped no-change outcome bound to the exact proposal version, preview hash and matched canonical target identity/state, with replay and stale-target checks and zero canonical/publication effects. Preserve legitimate reviewed declined outcomes rather than simply blocking all rejected requests. Completion needs structured truthful outcome presentation at its existing proposal/review boundary; do not silently rewrite human text or infer new customer-send authority. Prove duplicate replay, stale target, bare rejection, distinct KEEP_CANONICAL, and no-change presentation before exposing a new control. All broader campaign and external gates remain unchanged.

### Implemented no-change prerequisite: exact duplicate identity

Candidate `ce807aba` fixes duplicate classification before durable outcome work. A match on another row no longer hides a correction/supersession to a different or missing explicit target. Untargeted exact matches are selected deterministically by ID. The separate `duplicateMatch` preview field identifies the matched row without changing the meaning of `targetKnowledgeEntryId`. Duplicate preview hashes now include its exact semantic snapshot (ID, title, category, content, enabled state and authority), venue, relation and desired input; normalized equality alone does not freeze stale canonical bytes. Non-duplicate hash shape is unchanged.

Twenty-four classifier/scoped-service tests, API typecheck and configured lint pass, with independent review. See `docs/evidence/semantic-duplicate-target-preview-2026-09-10.json`. This is preview proof only. Durable reviewed no-change receipts, exact replay/source binding, lock-time canonical validation and truthful completion presentation remain the next connected implementation. Earlier native manifests containing this source remain historical at their recorded revisions.

## Durable reviewed duplicate decision

Candidate `104c5aea` adds `SemanticDuplicateResolution`, an immutable scoped receipt with exact approved proposal version, preview hash, matched canonical target and full canonical snapshot hash, original relation/desired input, bounded server-resolved support source references, reviewer note/identity and replay input hash. The admin-only `resolveSupportSemanticDuplicate` action acquires the venue content lock, locks the proposal, rejects other existing outcomes, validates source provenance and exact approved wording, then locks the matched target and recomputes the duplicate preview. It inserts the receipt and audit together. It never changes the proposal, canonical content, publication or completion state.

Support proposal bodies use a typed envelope followed by a runtime newline. Exact CREATE_KNOWLEDGE, UPDATE_KNOWLEDGE and NO_CONTENT_CHANGE encodings are accepted alongside unwrapped replacement bodies; arbitrary stripping, literal backslash-n, retirement and retrieval-correction envelopes are rejected. NO_CONTENT_CHANGE expressly remains a no-change decision and must never be presented by future consumers as a content update. Preview hashing retains the full stored proposal identity.

The database enforces scoped restrictive proposal/target foreign keys, one receipt per proposal, exact approved proposal timestamp, bounded source evidence and append-only storage. Middleware independently enforces tenant isolation and append-only behavior. Identical operation/actor/input retries recover the original decision; changed input, reviewer or operation/proposal collision fails. Replay reports `currentFulfillmentVerified: false`: it does not assert today's canonical state.

Native proof `bb0530e2873e` at `b4ebcd33` passes real preparation -> human approval -> duplicate preview -> concurrent admin submissions, exact stored source/target identity, zero canonical/proposal/handoff effects, middleware and raw SQL immutability, and the still-blocked completion gate. The existing completion lifecycle also passes. PostgreSQL stopped; all measured source bytes match at the checkpoint. There are 244 migrations and 262 tables. A prior fixture assertion failed only because it assumed lowercase error text; its retained result remains failed. See `docs/evidence/semantic-duplicate-resolution-native-2026-09-10.json`. Fifty-two API tests and 339 tenant/evaluation tests pass, with API/DB typechecks and configured lint.

### Next connected implementation at the durable-receipt checkpoint

At that checkpoint the durable decision was not yet completion evidence; the V6 reader below now supplies it. Add a bounded shared no-change fulfillment reader for exact support source proposals and explicit immutable outcomes, including the distinct existing KEEP_CANONICAL decision. Recheck exact proposal/source identity and canonical snapshot under compatible locks; verify current guest-visible state where claiming existing guidance already satisfies the request. Reject conflicting typed/package/temporal outcomes rather than dropping receipts. Preserve legitimate reviewed declined outcomes with explicit evidence instead of using bare REJECTED as a substitute. Compose a new versioned fulfillment identity and truthful structured completion presentation at the existing human review boundary. Keep human explanatory text intact and do not infer new delivery authority. Add the admin review control only with its current-state/replay/error behavior and representative rendered proof. Update older disposable runners' expected migration/table counts when rerunning them against this schema; historical proofs retain their original counts/revisions.

## Current no-change fulfillment evidence

Candidate `95c354b7` composes fulfillment version 6 with a bounded no-change evidence layer. DUPLICATE_NOOP and KEEP_CANONICAL retain distinct outcome identities. The reader uses exact frozen support sources and one-hop replacement lineage, locks proposal IDs and target IDs in sorted order under the existing request/venue locks, then checks the decision and current canonical state. Duplicate source references are rehashed against the bounded original support messages. A same-proposal typed, package, temporal or adoption handoff conflicts with a no-change outcome.

Duplicate decisions require the unchanged approved proposal version. KEEP_CANONICAL receipts retain the pre-retirement proposal timestamp, while the current proposal is rejected: the reader checks its exact resolver/reviewer/note binding and the answered question's retained timestamps/hash, and records the locked current proposal timestamp in fulfillment identity. It does not pretend the two timestamps are equal. The existing review routes change those markers when a new review occurs. Bare rejection still is not a newly implemented declined-outcome receipt; that separate legacy gap remains open.

Targets must be enabled, public, and match the complete canonical snapshot hash. Linked native projections additionally require complete module/revision/publication identity, the latest revision and PUBLISH event, public audience and a current effective window. The actual guest adapter must return matching content and provenance fields; matching a target ID alone is insufficient. Evidence includes native release/state identity and the exact projection/window. Verification clocks alone are normalized out of approval equality. A changed decision, canonical snapshot or guest state requires refreshed review. The final pre-CAS effectiveness check also covers no-change guidance.

The canonical target hash now lives in contracts; the old API export remains an alias with identical bytes and plain-object validation. Empty no-change evidence returns NOT_APPLICABLE without querying native state. New V6 approvals include the no-change identity; legacy v1 compatibility cannot bypass nonempty no-change evidence.

### Remaining connected presentation and reviewed-decline work

Nonempty no-change evidence is verifiable, but manual and agent completion deliberately remain guarded until structured client presentation is implemented. Do not remove the guard just to make an existing arbitrary completion body pass. The next slice should persist a structured completion outcome separately from the human explanation, bind it to the reviewed fulfillment, expose it through existing support reads, and render it clearly in client/operator conversation views. Preserve the human body exactly. Distinguish updated, no-change and mixed outcomes without deriving them from prose; choose the outcome from verified evidence. The review UI must show that outcome before approval. Existing generic completion-proposal preparation can still prepare a guarded outcome; address this together with presentation so users do not approve an unexecutable proposal.

Add the explicit duplicate-decision review control using current prepared preview/version, frozen retry input, historical receipt qualification and scope fences; do not infer new customer-send authority. Keep KEEP_CANONICAL separate from automatic duplicate detection. Legitimate reviewed declined requests need an explicit immutable outcome/evidence path before replacing the legacy bare-REJECTED exemption. Preserve original requirements, external reservations and provider/device/release gates. No frontend changed in the V6 evidence slice; representative rendered proof belongs to the connected presentation slice.

### V6 proof checkpoint

The V6 evidence implementation passes 155 targeted tests (60 DB, 37 contracts, 58 API), all affected typechecks and configured lint. Four final native runs pass: duplicate/target-lock race `be04f0d903b9` (49 sources), KEEP_CANONICAL plus resolved adoption `c085894dd4df` (51), addition/receipt-chain regression `4d6daef76cbc` (46), and temporal/expiry regression `eb53beeb1c83` (49). These are five distinct cases across seven executions because the existing completion lifecycle runs three times. All measured bytes match at this checkpoint; all PostgreSQL instances stopped. The legacy `243` directory label on some runner outputs is historical naming: current database read-back is 244 migrations/262 tables.

The duplicate race fixture at `4dd93442` holds an uncommitted canonical target edit, identifies the actual fulfillment reader backend, observes its Lock wait on the target query, then commits the writer and requires rejection with the changed-snapshot error. Both transaction promises have immediate settlement handlers and release/cleanup on failure. This extends the earlier passing non-concurrent duplicate proof; it does not change production code after `95c354b7`. See `docs/evidence/support-no-change-fulfillment-native-2026-09-10.json` for exact identities and limits. No-change client completion/presentation and reviewed-decline integration remain unfinished.

### Structured completion presentation integrated (2026-09-10)

At `5536515257f3387680a286716dae4ea1426fe097`, completion persists a separately derived UPDATED / NO_CHANGE / MIXED / RESOLVED outcome on the immutable client-visible message. The operator explanation remains unchanged. The manual UI prepares exact request-version/fulfillment evidence, shows the factual outcome before confirmation, and freezes the full operation/body/outcome/digest after an unknown mutation result. Execution rejects stale evidence or a mismatched outcome; NO_CHANGE/MIXED cannot bypass explicit structured review. The temporary blanket no-change completion guard is replaced by this binding.

New completion approval snapshots and grants bind the outcome. Founder review reads expose only the parsed completion body/outcome, never the raw snapshot, and display them before the decision. Historical grants retain their exact parameter shape for UPDATED/RESOLVED; historical NO_CHANGE/MIXED requires refreshed review. Existing historical messages remain nullable and receive no invented outcome. Client and operator conversation readers retain the stored label above the exact body.

Proof: 205 targeted checks; affected package typechecks and configured lint; native PostgreSQL 245 migrations/262 tables, reviewed duplicate completion/replay/target-lock race and negative outcome SQL constraints plus existing completion lifecycle; four rendered widths with synthetic transport, keyboard, loading/error/unknown retry, accessibility and overflow checks. See `docs/evidence/support-completion-outcomes-2026-09-10.json` for exact measured identities and proof limits. All owned servers stopped. This is separate native and rendered proof, not live/provider/customer or full campaign acceptance.

Next connected slice: explicit durable duplicate review UI using the existing preview, receipt action, source/version scopes, frozen retry input and historical receipt qualification. KEEP_CANONICAL remains distinct. Explicit reviewed-decline evidence is still needed before replacing the legacy bare-REJECTED exemption. Original requirements, external reservations and dependent provider/device/release gates remain intact. Broad handoff refresh remains deferred.

### Explicit duplicate review and outcome arbitration (2026-09-10)

Implemented at `1f116b636790d094b04c6bfeaf475ff9c40f8781`. Approved support-bound, enabled non-temporal DUPLICATE_NOOP previews now require an explicit review note and confirmation. Unknown results freeze the complete input and sibling controls for exact retry. Success is terminal locally; durable list and authoring reads show bounded historical receipt metadata and suppress draft actions. Proposal status is not mislabeled closed. Revision-current does not mean current fulfillment.

The 1300 migration closes all five reverse outcome paths through unique immutable SQL-internal `semantic_proposal_outcome_claims`. Proposal row locks alone were insufficient under Repeatable Read; unique claim arbitration closes that snapshot gap without banning the isolation level. Compatible CONTENT receipt combinations remain allowed. Migration rejects contradictory history before backfilling claims.

Proof: 101 targeted tests, affected typechecks/lint, four final native runs at 246 migrations/263 tables (five distinct cases/seven executions), and four rendered widths with keyboard, pending/unknown/exact retry, terminal controls and retained receipt cards. Real native races cover Read Committed and Repeatable Read. All measured bytes match and owned servers stopped. See `docs/evidence/support-duplicate-review-2026-09-10.json`; native persistence and synthetic browser transport are separate proof layers. No live/provider/customer claim.

Next: prove seeded upgrade/backfill and contradictory-history refusal on a new disposable copy, preserving all historical proof databases. Then implement explicit reviewed-decline evidence before replacing the bare-REJECTED exemption. Original requirements, reservations and dependent gates remain intact; broad handoff refresh is deferred.

### Reviewed-decline cutover constraints (2026-09-10; implementation still pending)

Source review found that removing the bare REJECTED exemption also affects originals retired by PROPOSE_REPLACEMENT. These are not human declines. The replacement must itself have verified fulfillment before the original can be considered resolved. Preserve KEEP_CANONICAL on its existing independently verified no-change path.

Implement the cutover as one coherent behavior change, with these boundaries:

- Add a separate append-only, tenant/venue/proposal-scoped reviewed-decline receipt. An explicit human action binds its operation, actor, exact before/after proposal versions, review time/note hash and bounded frozen support-message evidence. A fresh review may attest a historical REJECTED proposal; do not manufacture historical evidence from its mutable review fields or sparse audit log. Review and receipt insertion must commit together, with exact-input/actor retry recovery. The current reject route has no such receipt or replay guarantee.
- Arbitrate DECLINED against duplicate and all content/conflict outcomes using the same immutable unique outcome claim. Enforce both insert directions and old Repeatable Read snapshots. Do not classify KEEP_CANONICAL or PROPOSE_REPLACEMENT as DECLINED. Preserve source message retention/privacy bounds and use the existing source resolver.
- Add a bounded locked reader and a new fulfillment version, retaining old serialized contracts for historical parsing. Verify exact current rejection/reviewer/note/version plus receipt/source hashes. Pass only verified IDs into the content reader; bare status no longer grants an exemption. Preserve request -> venue -> sorted proposal locking, post-lock reads and exact request/source versions.
- Explicitly qualify a retired PROPOSE_REPLACEMENT original through its immutable resolution, exact answered-question evidence, current retirement markers and one-hop replacement identity. The replacement must belong to this request's successfully verified fulfillment set; reject missing, forked, stale or unverified replacements. Include retirement evidence in the new digest. Reuse existing immutable conflict evidence and source adapters; do not automatically add a required field to historical conflict rows or require an invented blanket backfill.
- A decline is neither a canonical update nor proof that existing guidance satisfies the request. Keep its review facts separate from duplicate/KEEP_CANONICAL facts. Decline-only completion can say Request resolved. For combinations, the prepared review must expose which items were declined and which were updated or already satisfied; do not blindly map decline + duplicate to a label implying updates occurred. Preserve the exact human explanation and derive labels from evidence, not prose. Do not expose the action before operator review and truthful completion presentation are connected.
- New completion approvals bind the new evidence and review facts. Retain completed operation replay as recovery of the original immutable result, not a recomputation of current fulfillment. Historical unexecuted approvals with newly required evidence must refresh; the old empty V1 fallback must not erase source-bound declines or retirement decisions. Test exact no-decline historical digest behavior before deciding whether current reads may keep V6 for that subset.

Required cutover proof: bare/historical rejection fails without fresh evidence; exact decline/retry succeeds with zero canonical/publication effects; wrong scope, changed actor/input, stale note/version/source fail; competing outcome insertion and races fail in both directions under Read Committed/Repeatable Read; KEEP_CANONICAL remains distinct; verified replacement resolves its exact retired original while pending/foreign/forked replacement does not; historical completed replay survives without new effects; stale unexecuted grants require refresh; decline-only and mixed review facts render truthfully. Keep the existing exemption until these legitimate paths are implemented together, rather than breaking them with a one-line deletion.

### Atomic arbitration upgrade proved (2026-09-10)

Candidate `89e23b4def56c9473af55ae5a09d8a74bba9777f` adds an explicit READ COMMITTED transaction and proposal/source-table write exclusion around the 1300 migration. This closes the interval between historical scanning, backfill and trigger installation. It also prevents partial schema state on contradiction failure. The migration was previously applied only to retained local disposable fixtures in this campaign; this change does not authorize or describe a hosted migration.

Fresh native proof `0ed590404a0d` passes 246 migrations/263 tables and two duplicate/completion lifecycle cases, including post-install Read Committed and Repeatable Read races. All 52 measured sources match the candidate. Seeded Prisma upgrade `c03d2539db8a` proves exact duplicate/content backfill and contradiction refusal with rollback. Writer proof `cc7b373014d7` streams the exact migration SQL through psql, withholds its trailing COMMIT, observes the exact competing INSERT backend waiting on a relation lock, then commits and verifies rejection by the newly installed claim guard. Both original source-tree hashes remain unchanged; all owned servers stopped.

See `docs/evidence/support-outcome-arbitration-upgrade-2026-09-10.json` for identities and limits, including the retained failed runner attempt whose aggregate PASS flag was wrong. The final runner requires each case's assertions to pass. The seeded cases cover one content receipt family, not every compatible historical combination. Earlier manifests containing the old 1300 bytes remain historical, while unchanged UI proof keeps its original scope.

Next implementation is the reviewed-decline cutover specified above, including explicit qualification of retired replacement originals and truthful mixed review facts. Broad handoff refresh remains deferred. No full campaign/provider/device/customer acceptance is claimed.
