# Weekly report activity proof

The P08-06 native fixture at `9eb39b77` drives the canonical durable draft request, persisted execution claim, actual weekly-report processor and scoped MCP lifecycle. The three fictional venues have six, one and zero public sessions/messages. The deterministic provider is invoked only for nonzero activity. An invalid source citation is dropped, a valid citation survives, and private SECOND_LAYER evidence is excluded. The high prompt also excludes the sibling low venue question.

All reports remain generated DRAFTs, with CONSUMED dispatches, COMPLETE job records and NOT_PUBLISHED lifecycle state. No operational-event delivery or non-report job exists for the isolated tenant. Generation is not publication or email delivery.

Retained proof `c3767a0bcf4f` passes one combined case on PostgreSQL 16.15/pgvector0.8.6 with 247 migrations/264 tables. All 32 measured source bytes match the committed candidate; PostgreSQL stopped. The existing 33 worker unit cases, API typecheck and owned lint also pass. Exact paths/hashes and the failed fixture attempt are recorded in `../evidence/weekly-report-activity-native-2026-09-10.json`.

This closes the missing local high/low/zero request-to-processor lifecycle proof, not all P08-06 acceptance. Captured-answer populations are zero here; snapshot concurrency and sample-cap boundaries remain separate. Redis transport, live-provider insight quality, human usefulness and customer publication/delivery are not proven. Existing first-week external assignments and original 128 requirements, 32 QA cases and A/B/C gates remain unchanged.

## Captured-answer snapshot and sample bounds

Candidate `e490c2fd` adds native proof through the unchanged production source reader. The actual Repeatable Read transaction counts 99 public, non-AI captured answers. A test-only query timing proxy pauses the real sample query until an independent writer commits three earlier-in-window answers. The retained transaction still returns exactly the original 99; a fresh read counts 102 and returns the expected first 100, including all three new answers. Exact scoped interception counters prevent a missing barrier from being mistaken for concurrency proof.

A subsequent multibyte long-answer read retains the full count of 102 while reducing the sample below 100 to keep response JSON within 30,000 UTF-8 bytes. Private and sibling evidence is excluded. AI-invented questions do not contribute captured-answer statistics, while their genuine public visitor messages may remain general evidence.

Proof `86e18dc30a3c` passes on 247 migrations/264 tables with all 32 measured source bytes matching the committed candidate; PostgreSQL stopped. API types and owned lint pass. Two fixture instrumentation failures are retained and excluded. See `../evidence/weekly-report-captured-answer-snapshot-2026-09-10.json`. This closes the previously missing controlled captured-answer snapshot/cap proof. It does not make every other report field part of that snapshot or establish live insight quality, Redis transport, customer delivery or complete P08-06 acceptance.

Candidate cleanup `7fa53fd8` removes five unused imports that previously blocked full API lint. Full configured API lint and API typecheck now pass. No behavior change or new media-native acceptance is claimed from this cleanup.
