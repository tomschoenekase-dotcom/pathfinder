# Torchiko staging current truth — 2026-08-31

> **Migration instruction status: HISTORICAL — DO NOT EXECUTE.**

This is the bounded current snapshot of the authorized Railway `staging` environment. It records
exact deployment evidence; it does not authorize production, provider activation, customer contact,
live billing, destructive data work, or deferred marketing, branding, wordmark, or Tochi work.

## Active admitted release

- Git revision: `46e404777f197e53bfb181b9953e25423e72aa54`.
- Exact-revision GitHub Actions run `33362817617` passed.
- Web deployment: `4deb23dc-7ae9-43a0-bd85-f1967a8f63e9`, image
  `sha256:b46a4f26a4f77e5b594ba0417b75d2e47a86d13f698f98f87eb475d372a0ac05`.
- Dashboard deployment: `4bbc1c5c-533d-4ebd-9301-24e87f0ef8fe`, image
  `sha256:d9136e40a68a830b85b98e07b96ea6fffd621b4ddd59aeab1570787747cb65a4`.
- Worker deployment: `480bad12-9406-4123-8cab-f00bc70a1202`, image
  `sha256:99993bf8a54f6b6c74cf743dbf8d0846c3ce52edd8a0122c57e683d968c1e981`.

The guarded pre-deploy admitted the frozen 206-file migration manifest and exact complete 206-row
ledger, rechecked 206/206 integrity and 232 public tables, and returned the one-run migration
admission to `0`. The exact hosted staging profile passed 19/19 with zero failed or blocked. Public
health reported the exact release and reviewed dependency identities with database and queue `up`.
Production was not selected or mutated.

## Fresh liveness and runtime proof

On 2026-08-31, the fail-closed topology verifier read Railway status through the explicit
`@railway/cli` package and admitted exactly one deployment for each required service. Every deployment
was `SUCCESS`, every service had exactly one `RUNNING` instance, and every revision and immutable
image digest matched the identities above. The bounded runtime audit separately found zero service
error rows, zero web/dashboard HTTP 5xx rows, zero founder-capture failures, and retained complete
founder-absence observations without retaining raw provider logs or request data.

## Prevention head remains separate

Engineering head `f5f5483402d209becb00c9c859c1d23e417a0952` passed local candidate verification
27/27 and GitHub Actions run `33412486400`. It adds fail-closed topology/runtime-audit prevention and
an asynchronous test-stability repair, but it is not deployed. The active staging truth remains the
exact admitted `46e40477...` release until a separately admitted rollout replaces it.

This snapshot does not prove production, current backup/PITR recovery, provider-backed model quality,
authenticated operator/client journeys, physical-device behavior, customer contact, or live billing.
Every future release must repeat exact migration admission, service topology, health, runtime, and
hosted-profile verification.
