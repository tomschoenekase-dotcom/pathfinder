# Native evidence admission beyond the five pilots

**Implemented and exercised on the existing nonpilot CRM record. SEND AUTHORIZED: NO.**

**Workspace limitation:** the final exact restoration of Next's temporary `next-env.d.ts` route-type pointer was blocked before execution. The original byte backup is retained, `tsconfig.json` is unchanged, and neither generated file is committed. Do not describe that restoration as complete. Product/native/browser acceptance passed independently of that blocked housekeeping operation.

## Source and assignment

Worktree: `C:\Users\tomsc\Downloads\PathFinder-crm-research-20260919`

Branch: `codex/torchiko-crm-research-20260919`

Source implementation commit: **`553261252b9bb58d9096998c492c07c903496dc0`**.

Starting delivery: `a67a1cb60093ec1a96a1272e4b3a36a08e2012ae`; the existing meaning-review implementation `d67a625b2121144e674a86822c2ff667f80b0664` was extended, not rebuilt. Resolve this handoff's subsequent evidence commit with `git log -1 --format=%H -- docs/crm-evidence-admission-20260921/HANDOFF.md`; the compact vault pointer records it after commit.

The full assignment was read from `AwesomeVault\95 AI Staging\Overnight Coordination 2026-09-20\primary-coordinator-20260921\morning-r003\01_CRM_Native_Evidence_Admission.md`. Applicable project/vault instructions, the source-context skill, exact predecessor handoffs and original Gate/Composer contracts were read. The worktree has no root `AGENTS.md`; its `CLAUDE.md` and vault instructions were used. The bounded source-context packet is recorded in `artifacts/crm-evidence-admission-20260921-r001/EXECUTION.md`; it was evidence about a predecessor, not authority over current source.

## What is now implemented

An existing native prospect can retain a bounded official-page capture and explicitly select its factual claims and public route for a task. This uses the existing `ProspectSourceEvidence`, exact component storage, strict audit and activity owners. No parallel source/review database or migration was introduced.

The native UI now shows capture provenance, native/import identity, source URL, genuine observation/retrieval dates, quoted source locators and captured-page hashes. Its admission action accepts existing capture/claim/route IDs and separately attributed task direction. It does not accept caller “verified” flags, arbitrary file paths, raw source-fact prose, a URL to fetch, human approval or send authority.

The private bridge feeds the same Research Gate, narrowly extended original Composer assembly, original WLT, real Approved Language library, native immutable draft and existing meaning-review path. The native prospect uses its real CRM identity; Composer `pilot_id` is null. The previous five pilot cases remain compatibility cases, not the admission mechanism for this prospect.

Changing selected source facts or the recipient/form route makes applicable old preparations/drafts/assessments stale without rewriting their histories. Stale or contradictory used claims remain held by the original Gate. Optional unused volatile claims do not trigger research merely because they exist in a capture. A newer capture does not silently refresh an older selected source's dates. Email and form alternatives are distinguished from genuinely conflicting assertions about the same selected route kind.

`ADMISSION-CONTRACT.md` documents the exact owners, trust boundary, bounded schemas, idempotency/concurrency semantics, freshness behavior and first-slice limits. `SOURCE-IDENTITIES.json` records the 26 committed source/test/helper/document paths, current native runtime hashes and exact upstream before/after identities. `ACCEPTANCE-RECEIPT.json` binds that source to the executed checks and retained evidence, including the generated-file restoration block.

## Genuine nonpilot evidence and native identity

The technical-QA target is **Centralia Historical Society Museum, Centralia, Missouri**:

```text
organizationId: porg_00061105e8861987c4b5210b
venueId:        pvenue_00061105e8861987c4b5210b
importRecordId: cmuanwb5u04738teslapg9yyt
sourceLocator:  Columbia-Jeff City!row:9
captureId:      native-capture_b06cf5f02caf9fb4ebce85e20384224bc89d2562
```

`before-native.json` recorded the actual Research Gate's four missing-evidence questions before any page capture. Its plan is `plan-8c1d2b108099b56e7ad439a4c065e5bde49edd46cba85a02f00e4132d56d3b9a`. The exact pre-capture receipt SHA-256 is `06614280dcd064bba1dbebe618740686d7799265f7c57a39869f7368e22f5748`.

The assignment's three-page-capture allowance was used exactly: one official-home-page browser-tool inspection and two retained foreground HTTPS GETs, for the official home and contact pages. There were no further requests, link walks, bulk research or product crawler. Retained genuine UTF-8 HTML bytes, response metadata and normalized-text locators are under `captures-r001`; `capture-record.json` binds them to the actual native/import identity.

The home and contact page captures were observed at `2026-09-21T18:59:44.246324+00:00` and `2026-09-21T18:59:45.088118+00:00` respectively. Their exact bytes hash to:

```text
home:    019a8ad1a2e9dfaaf48c72548da3ad37b65f9eea0279171e124d0ae62af440a0
contact: 752dd91dc0dc2af8d319a9b72b082e85f7e5aa7da90713ffeb707abfa62627d7
```

The association uses the contact page's explicit museum name and Centralia, MO address, plus the home page's named link to that identity page—not merely a shared domain. The published email matches the retained imported candidate, but readiness and permission remain UNKNOWN. The original imported Union Hotel description was **not** used as verified source evidence or silently corrected. Exact public-form markup supports a separate form-route selection; no form was submitted.

The source pages are genuine. The selection, draft text, deliberately wrong assertions and model assessments are **technical-QA actions**, recorded as explicitly synthetic SYSTEM activity. This is not a claim that Tom chose to contact the museum, that a human authenticated the assessment, or that the venue replied.

## Narrow upstream Composer change

Only these three files changed in the explicitly authorized existing Composer owner:

```text
implementation/composer.py         — backwards-compatible optional native catalog input
implementation/native_catalog.py   — bounded identity/quote/byte catalog contract
tests/test_native_catalog.py       — read-only catalog and retained-bundle compatibility tests
```

No validator was forked or monkeypatched. `resolve`/`fact_catalog` retain their pilot behavior, and the file-bundle `prepare`, `verify_preparation`, `validate`, `export_review` and filesystem guards are unchanged. The default five pilot assemblies produce the same exact preparation metadata/file hashes; all five original review bundles also pass the original `validator.validate` path.

The scoped patch and exact pre/post images are retained in:

```text
artifacts/crm-evidence-admission-20260921-r001/composer-seam-r001/
  composer-native-catalog.patch
  receipt.json
  preimage/implementation/composer.py
  postimage/implementation/composer.py
  postimage/implementation/native_catalog.py
  postimage/tests/test_native_catalog.py
```

`git apply --check` passed against the exact retained preimage, without applying the patch again or creating another worktree. Patch SHA-256: `2a1a06af9108a902603cf08da2a602327234b84ec96a6046e181a91fece6c5fa`. The actual upstream files already contain this implementation; do not apply it blindly to the already-updated owner. Before/after identities and the exact verification command are in the seam receipt.

Of the 234 original upstream files fingerprinted before work, **233 are unchanged**; the sole modified original is the allowed `composer.py`. The new catalog runtime and its test are separately identified. Historical pilot/contact data, WLT, Approved Language, Research Gate and correspondence implementations remain unchanged. No broad vault staging occurred. Relevant code revisions intentionally invalidate old native preparations; reprepare through the existing owner rather than reimporting or resetting history.

## Executed verification

Artifact root: `artifacts/crm-evidence-admission-20260921-r001`.

| Verification                                                           | Final result             |
| ---------------------------------------------------------------------- | ------------------------ |
| Bridge/native-catalog/original meaning integration tests               | 43 passed                |
| Original Composer catalog and five retained-bundle compatibility tests | 4 passed                 |
| Native database helper tests                                           | 30 passed                |
| API authority/schema tests                                             | 37 passed                |
| UI interaction/accessibility unit tests                                | 16 passed                |
| Total focused tests                                                    | **130 passed**           |
| Native local end-to-end acceptance, `native-full-r002.json`            | **28 checks passed**     |
| Real Edge journey, `browser-r003/receipt.json`                         | **56 checks passed**     |
| Final post-browser read-only native preservation/count check           | **3 checks passed**      |
| Database, API and dashboard typechecks                                 | All three passed         |
| Raw-SQL, tenant-bypass and public-surface boundary scans               | All three passed         |
| Scoped Composer patch application check                                | Passed, no patch applied |

The tests cover wrong native venue/lineage, explicit location versus shared-domain collisions, raw-byte/hash/quote/offset tampering, caller “verified” flags, arbitrary extra paths, future/stale dates, used versus unused expired claims, conflicting current results, consistent recaptures with email/form alternatives, inert source instructions, unresolved/form routing, idempotent replay, source-change invalidation, suppression and denied agent/sender authority.

Native concurrency was exercised with exactly two simultaneous admissions from one expected source-selection head: one append succeeded and the other transaction conflicted. The expected Prisma conflict is retained, not hidden as a clean log. Exact capture replay does not refresh its observation dates or rewrite native contact state.

The actual UI journey used the original prospect directory/detail: inspect source → keyboard admission → original Gate → actual WLT with zero approved entries → exact bad draft → recorded unsupported exhibit/$25 price/completed-visit findings → separate read acknowledgment → corrected immutable revision → original meaning assessment → reviewed-no-send. It also changed the source selection, inspected stale historical meaning evidence, selected a real form URL, held an unresolved route, and completed a fresh independent review after returning to the public email route.

Desktop/narrow layouts were exercised at 1440, 768, 375 and 320 pixels. Three real-browser accessibility scans found zero violations. Seven screenshots are retained in the final browser directory; representative desktop, narrow evidence, stale-review and form-route renders were visually inspected. No new uncaught JavaScript/hydration errors occurred. The underlying browser was the installed Edge with external page requests blocked; the Desktop connector itself was unavailable.

All groups ran serially; Vitest used at most two workers. Full Next production builds, broad upstream mutation suites, a new 42-table database sweep and real-device Safari/touch testing were not performed. They are not claimed by these receipts.

## Preserved data and limits of the preservation proof

The final read-only receipt rechecked this prospect's exact organization, venue, contact and import fields/IDs, including timestamps and lineage. Their combined fingerprint remains:

```text
2bf611889f92baf4a84c9b8c036d45470f3915155145027bdeacbbfbed4abf3e
```

Every original source-evidence row for this prospect remains identical. Native acceptance also compared the earlier reviewed draft and meaning-receipt full rows before/after route changes. Changes append history rather than overwrite it. No workbook reimport, campaign, send batch/item/outbox, enabled provider, approved draft or real message was created. Final aggregate counts for those prohibited outcomes remain zero. The source capture itself is protected through the existing append-only native owner and verified byte/hash bindings; no new SQL capture trigger is claimed.

This is bounded preservation proof for the selected prospect and protected source/library files, not a newly executed full-database proof over all 16,725 prospects or 42 tables. The predecessor's blocked broad fingerprint operation was not retried through an alternate route.

## Retained failures and cleanup boundary

The first browser run correctly held the bad meaning assessment, but its nineteenth assertion mistakenly assumed that the existing read-review draft state could not be `REVIEWED_NO_SEND` while meaning remained BLOCKED. The accepted predecessor explicitly permits those independent states. Only the new harness assertion was corrected; the original owner was not changed to manufacture a pass. That failure receipt and its initial missing-evidence UI proof remain in `browser-r001`, with `ASSERTION-RECONCILIATION.md`. The final complete journey passed in `browser-r003`.

A formatter check flagged only the browser harness's wrapping of that corrected Boolean assertion. The final AST comparison confirms unchanged syntax structure; two initially unsuitable comparison probes are retained. No runtime file changed during that formatting reconciliation. Normal pre-commit hooks ran. New immutable artifact/data paths are excluded from formatting/newline rewriting via narrowly scoped `.prettierignore` and `.gitattributes` entries, not a disabled hook.

The original preview helper's diagnostic process enumeration encountered a recycled parent PID and listed two older Apple helpers. Windows stopped only the six owned preview processes; both older helpers were immediately verified still running unchanged. The helper now requires chronological parent/root creation-time consistency. The final preview stop receipt contains only the correct owned processes. Both stop receipts and `OWNERSHIP-RECONCILIATION.md` are retained.

Session discovery and Desktop observation returned actual `tool not found` errors. No cross-chat lock-registry or Desktop-control proof is invented. Core source/process inspection and the explicitly assigned exclusive ownership boundary were used; browser proof came from the actual installed Edge.

Finally, the attempt to restore Next's temporary generated pointer was blocked by the tool before execution. It was not retried through another command/path/tool. `generated-restore-BLOCKED.json` records the exact failure and byte difference. Current and original values:

```text
next-env.d.ts original backup: 6902ed943c3fc642ee66cae54f04765d7b3331309d39c79b241bd3578a440ea4
next-env.d.ts current preview: fca437bdf20443bbdc6cf552a32c1bc66b9926b4443ad5deb572d22edf1dc370
tsconfig.json unchanged:       127b6f212c9527cc51931abb193204b1ca4357910029450dc4af04858140a45c
```

The original backup is `preview-r001/next-env.before.txt`. The current file differs only in the route-type reference (`.next-crm-research` versus `.next-crm-build-final`) and that line's LF→CRLF terminator. Original backup bytes, including their mixed terminators, remain intact. Both generated files remain outside the commits. A future legitimately permitted housekeeping step must reconcile and restore only that known preview delta; this handoff does not mark it done.

The final observed machine state at 19:51:01Z had **8.499 GiB free disk** and **7.557 GiB available RAM**. The owned browser and both preview process trees are closed; port 58618 is released. The same retained database container `39d369dae3d0` remains running at `127.0.0.1:58617`. No worktree Node/Python process remained.

## Open and continue

Use the existing guarded command from this worktree:

```powershell
powershell -NoProfile -File scripts/run-local-crm-research.ps1 -Mode preview -EnableSalesPreparation
```

The local review route is `http://127.0.0.1:58618/dev-fixtures/prospect-research`; search for Centralia Historical Society Museum. Retained captures and task selections are already installed. Do not rerun the foreground collector or reset the technical-QA history to recreate an empty state. The fixed capture helper is not a general website-fetch endpoint.

The next ordinary product extension is explicit source/conflict or nonpilot-thread handling through the same owners—not a sender, recipient campaign, model-generated “verified” fact, language approval or another CRM. **SEND AUTHORIZED: NO.**
