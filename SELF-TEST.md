# Tom's Test Venue — no-send preparation and later authorized test

## Lane 3 checkpoint — 23 September 2026

The fixture and native draft-v6 evidence below belong to the preserved predecessor
`C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-connected-outreach`, not a
new successful Lane 3 run. Its original database, model result and older versions
were not reseeded or modified. Lane 3's actual HTTP acceptance remains HELD;
see `AUTHENTICATED-OUTREACH-ACCEPTANCE.md` and current `CODEX-START-HERE.md`.
The isolated Lane 3 HTTP fixture targets only `qa@example.invalid`, not a real
inbox. Its two created database resources were discarded after failed/held setup.
No Lane 3 native saved draft, authenticated company mailbox or send was proved.

**Current state: NO LIVE SEND AUTHORIZED.** Sender is
`tomschoenekase@torchiko.com`. No receiving inbox is assumed or inferred from
Tom's name, connected personal Gmail, old transcripts or test fixtures.

## Prepared now

The isolated native acceptance database
`torchiko_outreach_acceptance_20260923_r001` contains:

- organization `SYN-TOMS-TEST-ORG`;
- venue `SYN-TOMS-TEST-VENUE`, named **Tom's Test Venue**;
- no recipient contact, no claimed physical address, no fabricated county or
  public source, no mailbox provider account and no sending approval.

This is a private synthetic fixture, not a customer venue or published client.
It was created only in the new empty acceptance database. It was not added to the
retained real prospect dataset and does not prove a hosted/self-email connection.

The actual Codex/native writer proof is a different already-existing fixture,
`SYN-CRM-FIRSTSEND-VENUE-r100`, routed only to `fixture@example.invalid`. Its
native review draft is version 6. That invalid recipient must never be used for a
real send. The proof's exact text, immutable receipt and recovery are recorded in
`../../20260923-connected-outreach/qa/NATIVE-CODEX-PROOF.json`.

The original model result has also been retained and reopened unchanged by three
fresh CLI processes. Native rollback acceptance reopens both existing groups,
checks human pause/cancel/resume and exact lease/import recovery, then verifies an
identical before/after database digest. See `DELIVERY.md` for the latest receipts.
Neither proof authenticates the self-test inbox, company mailbox or deployed CRM.

The launch-scoped MCP adapter is prepared, with only native no-send preparation
tools. Its local status is `AUTHENTICATED_NATIVE_BRIDGE_NOT_CONFIGURED` until the
existing runtime owner admits the exact target, credential and selected native
run. The foreground adapter now obtains/heartbeats session and lease references
through native owners; it does not issue credentials or create runs. A successful
MCP initialization is not a successful live self-email.

## Required before the later live test

1. Tom explicitly names and confirms ONE receiving inbox he owns. Record the exact
   confirmation in the native human decision/evidence owner. Do not guess ownership
   from an email address, add aliases or select personal Gmail as the sender.
2. In the approved private runtime, locate the existing company provider account
   for `tomschoenekase@torchiko.com`. If unconfigured, ambiguous, expired or disconnected,
   complete its existing company-account connect/reconnect path under human control.
   Do not print tokens, make a parallel provider or enable delivery just to test login.
3. Establish the real native CRM bridge/admin authentication and scope, plus the
   private WLT/reference component runtime. Codex's ChatGPT subscription login is
   neither CRM authorization nor Gmail authorization. Reconcile code with the
   staging owner; a reviewed source patch is not deployment approval.
4. Use the same native one-venue read → selected WLT/reference → actual model
   draft → immutable import → exact reopen path. Record the real recipient,
   subject/body/hash, attachments (normally none), draft/version and current source
   snapshot. Resolve native research, contact and complete correspondence holds.
5. Tom reviews those EXACT bytes and approves that ONE message for that ONE
   recipient. Read acknowledgement and meaning review alone are insufficient.
   Any changed recipient, text, attachment or stale source invalidates that approval.
6. Only a separately authorized release may use the existing first-send/native
   campaign/outbox owner. Never reparent or mutate a permanently no-send draft,
   remove local no-send guards, create a second queue, send via a shell/CLI Gmail
   shortcut, or broaden this to a campaign.

## Expected proof on that later authorized run

Retain the native handoff, campaign/send item, provider account, exact message hash,
idempotency key and provider identifiers. Distinguish queue acceptance from provider
acceptance, provider Sent, receipt in Tom's inbox and an imported incoming reply.

If a send response is lost or ambiguous, reconcile the SAME retained outbox item
through the existing recovery owner. Do not make a new message/item or retry by
blindly resending. A failed auth check is not a failed message delivery.

Tom then replies from the confirmed receiving inbox. Use the existing company
mailbox synchronization/reconciliation path, not a copied email text fixture.
Verify the exact provider thread/message identity, source-retention state, native
organization/venue association and correspondence display. Ambiguous associations,
wrong mailbox, expired body, missing body or stale sync are explicit holds.

Finally reopen that same venue in a fresh Codex session, read the actual retained
incoming message and write/import a new reply REVIEW draft with the installed
WLT/reference. It is not authorized to send merely because the first message was.
Record evidence of each successful and held boundary rather than marking the
entire workflow “connected” on the strength of a login or a fake-provider test.

## Stop conditions

Any non-owned/unconfirmed inbox, wrong sender, stale or changed exact approval,
missing native grant, incomplete reply source, active no-send guard, unavailable
delivery owner or ambiguous transport outcome stops the corresponding action.
Continue independent no-send preparation/review work; do not bypass the boundary.
