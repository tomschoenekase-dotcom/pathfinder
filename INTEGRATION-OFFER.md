# Closeout B exact-source offer — CURRENT r002

Use `..\offer\r002\README.md`, `SOURCE-MANIFEST.json`, `native-claim.patch` and `PREREQUISITES.json`. The sole applicable shared-source offer is four exact-run-selection source/test files, zero migrations. It applies/reverses in a private index against observed A53d4366e; A was not edited. The rest of the native writer stack is an enumerated prerequisite decision, not an automatic import. Neither the whole261-file predecessor nor the public CI export is offered for integration.

Final native HTTP proof now passes26checks, with47Node/46native-review cases and complete exact-source receipts. Private target/authority, installed Codex acceptance and authenticated visual review remain separate gates. Read `CLOSEOUT-B-DELIVERY.md`. Existing r001 is retained unchanged; the text below describes that older package, not current proof.

## Archived Lane3 r001 offer

Owner: `lane-03-authenticated-outreach`.
Source root: `C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-four-chat-lane-03-outreach\worktree`.
Exact frozen result, SHA256 path list and clean/dirty state:
`..\offer\r001\SOURCE-MANIFEST.json`.
Base for the narrow patch: `fe205d86961186225fed24836ed155a941acada9`.
Patch: `..\offer\r001\source.patch`. Per-family patches are in that same folder.

## Offered path families

`connection.patch`: existing MCP/Codex/stdio integration plus the foreground
connection module and its Node tests. Depends on the native exact-run admission
below and the already-preserved native writer/credential owners. It adds no
credential issuance, run creation, mailbox, writer engine or outbox.

`native-claim.patch`: only `packages/api/src/agent-bridge/registry.ts`,
`packages/db/src/helpers/agent-bridge-actions.ts` and its focused test. Optional
runId narrows selection; existing worker requests without runId are unchanged.
This shared-owner edit requires Lane 4's deliberate review, not automatic copying.

`review.patch`: existing `ProspectWriterRoundtrip.tsx` and its test only. Explicit
uncertain receipt, retained original candidate, exact recipient and long-text
wrapping. No new review page or hidden approval.

`acceptance.patch`: the new provider-dark native HTTP acceptance runner/script
and focused Vitest configuration. Harness execution is still held; it is not a
production codepath or evidence of a working connection. Its task-resource helper
receipts/scripts remain outside the worktree under `qa` and are separately bundled
in the offer's source-only `validation-support.zip`.

`handoff.patch`: current start-here, delivery, matrix, integration documents,
no-send self-test context and local skill pointer. The installed global skill was
not repointed. Fresh operating-route admission must preserve this distinction.

## Dependencies and exclusions

Do not cherry-pick the preservation commit or apply the full 261-file archive to
the product checkout. It includes substantial inherited source, schema and worker
changes. First reconcile against Lane 4's newest source and review the enumerated
requests in `INTEGRATION-REQUESTS.md`. A private-index clean patch application
proves mechanical compatibility only, never shared runtime or authentication.

This offer excludes all `node_modules`, the frozen private generated client,
runtime `.outreach-retry` contents, secrets/env files, databases, browser profiles,
real prospect records and predecessor model text. Source-only validation support
must be reviewed before execution; path constants bind it to this isolated lane.

## Acceptance and ownership

Actual HTTP → installed Codex → authenticated review is still held. Lane 4 may
inspect/adapt these exact source hunks but must not mark outreach accepted or
switch a user-visible installed entry based on them. Run final native/UI/type/build
checks before promoting the offer. No production/staging mutation, credential
activation, model spend, mailbox change or send is authorized by this package.

No worker chats were launched. All task-owned test resources were cleaned up;
no heavy slot remains claimed. Rollback is the narrow reverse patch after checking
the live path hashes. Never reset later source or remove retained native drafts.
