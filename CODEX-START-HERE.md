# Torchiko Closeout B — CURRENT entry

**Read `CLOSEOUT-B-DELIVERY.md` first.** Final actual native HTTP is **26/26 PASS**, final Node **47/47 PASS**, native/review tests **46/46 PASS**. Full exact schema generation and the unchanged full migration chain passed. The installed private route is still held at **PRIVATE_NATIVE_TARGET_ADMISSION_REQUIRED**, not at the old RAM/HTTP implementation blockers.

Operating code: **ac33b42cc3a77bcbf8d53b7b6493d6332d12401a**, tree **d20826815cc3da08d822b37c7f1604d2fdb7ef83**. Work in this existing Lane3 checkout. Current exact offer/receipts: `..\offer\r002\SOURCE-MANIFEST.json`; final documentation-only checkout identity: `..\offer\r002\CHECKOUT-RECEIPT.json`. Keep the product and private outreach checkouts independent; do not import the261-file predecessor or CI export into A.

The exact remaining human action is in `..\offer\r002\PRIVATE-OPERATOR-GATE.md`. Existing Codex login and installed WLT/reference bindings are verified, but no live native host/tenant/venue/credential/run was legitimately admitted. No global skill repointing, new chat, model spend, mailbox activity, approval or deployment occurred. Preserve normal launcher sandbox/approval settings.

The section below is **the archived87f972f9/r001 checkpoint**, retained for evidence history only. Its HELD/NOT RUN test statuses and RAM-retry commands are superseded by the current closeout; do not execute them merely because they appear below.

---

## Archived pre-closeout implementation checkpoint

**READINESS: HELD. Source implementation exists; actual authenticated HTTP,
installed Codex through that new lifecycle, and authenticated browser acceptance
have NOT passed. No send, send approval, mailbox substitution or deployment.**

## Exact owner and evidence

Work from `C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-four-chat-lane-03-outreach\worktree`.
Branch: `machine/torchiko/20260923-four-chat-lane-03-outreach`.
The narrow final offer, exact commit/tree, path hashes and patch verification are
in `..\offer\r001\SOURCE-MANIFEST.json` and `..\offer\r001\source.patch`.
Read `DELIVERY.md`, `AUTHENTICATED-OUTREACH-ACCEPTANCE.md`, `INTEGRATION-OFFER.md`,
`INTEGRATION-REQUESTS.md`, then this checkout's
`.agents/skills/torchiko-outreach/SKILL.md` and `runtime-profile.json`.

The prerequisite preservation snapshot is `fe205d86961186225fed24836ed155a941acada9`,
tree `2ef4a76be03a218f95527d6162b12994cb34e6a5`, on original base
`8610d0d48cd8e6fdaff40c74421f5f491f8fe119`. All 261 selected predecessor files
matched both the frozen archive and live source when reconstructed. This entire
snapshot is NOT a staging cherry-pick authorization. Original dirty CRM source,
databases, real model result and older draft versions remain untouched.

The installed user-level outreach skill has NOT been repointed. This local skill
is the owner for work in this checkout; do not silently follow the installed
pointer back to `20260923-connected-outreach` and mistake it for this new source.
The predecessor's actual Codex draft-v6 and exact-recovery evidence remains at
`C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-connected-outreach\qa`.
It proves earlier native persistence, NOT this new authenticated HTTP lifecycle.

## Resume in this order, without rediscovering the vault

1. Read the newest Lane 1–4 statuses under
   `C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-four-chat-review\coordination`.
   Reconcile this offer with later changes. Do not reset another checkout, admit
   the 261-file archive wholesale, launch workers, or change hosted configuration.
2. Acquire the existing shared heavy slot and preserve at least 5 GiB free RAM.
   Own `..\qa\run_bounded.ps1` claims/releases the slot atomically, records exact
   process/log identity and stops only this run's descendants before the reserve
   is consumed. Never stop another lane, an old browser, Codex or a shared service.
3. Run the final focused checks and the actual native HTTP acceptance below.
   The latter must produce `HTTP-ACCEPTANCE.json` with `passed:true`, native HTTP
   events and immutable retry evidence. A migrated database, Node unit pass,
   local MCP initialization or synthetic SQL draft is not this proof.
4. Only after that passes, exercise the pinned installed Codex route with the
   same legitimately admitted private authentication. Keep normal on-request
   approvals and workspace-write sandbox. Do not run a later aggregate wrap-up
   chat automatically, spend on a model without authorization or relabel the
   harness's explicitly hand-authored fixture text as installed Codex output.
5. Finish actual desktop/tablet/keyboard review in the normal authenticated admin
   page; then offer an exact source to Lane 4 for broader type/build/integration.
   Neither a source offer nor staging acceptance authorizes production or email.

```powershell
Set-Location 'C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-four-chat-lane-03-outreach\worktree'
& '..\qa\run_bounded.ps1' -Script "$((Resolve-Path '..\qa').Path)\check_light.mjs" -Label 'node-lifecycle' -HeadroomGiB 0.25
& '..\qa\run_bounded.ps1' -Script "$((Resolve-Path '..\qa').Path)\run_focused.mjs" -Label 'focused-ui-and-native-contracts' -HeadroomGiB 0.5
& '..\qa\run_bounded.ps1' -Script "$((Get-Location).Path)\scripts\run-authenticated-outreach-http.mjs" -Label 'native-http-acceptance' -HeadroomGiB 0.75
```

Run these individually, not in parallel. The private native client is frozen and
admitted only for 503 exactly matching model/enum definitions; three unrelated
county/intake models are denied by the harness. See `..\qa\NATIVE-CLIENT-ADMISSION.json`
and `NATIVE-CLIENT-LINKS.json`. This is NOT a current full-schema generated client.
Full generation was resource-aborted. Do not regenerate into a borrowed/shared
package directory. A full application build needs its own current-schema client.

The native runner uses a create-only randomly named, task-owned, provider-dark
PostgreSQL container, a separate migration principal and restricted runtime
principal, existing native migration runner, real route and real credential
owners. Its cleanup removes only its own labeled resource. Failed setup attempts
are retained as failures, not discarded from the evidence history.

## What changed in the foreground connection

The existing launcher/MCP transport now owns the foreground session/worker and
the lease for ONE already-authorized selected native run. It uses existing native
register, exact-run claim, heartbeat, prospect-call and settlement owners. It never
issues/activates credentials, creates a run, guesses scope or grants access.
The operator does not manufacture session/lease IDs. Legacy pre-admitted attached
leases remain supported without taking over their other owner's heartbeat.

Native import still retains exact bytes before HTTP. A missing response is an
unknown outcome: recover/retry that same request, never make a replacement draft.
A later uncertain import cannot settle from an older receipt. Only a confirmed
immutable receipt permits successful no-send settlement; otherwise the native
failure/retry owner receives the same run. Cancellation/expiry stops use.

`node scripts/torchiko-outreach-codex.mjs doctor` is the installed-route diagnostic.
`node scripts/torchiko-outreach-codex.mjs start` now refuses missing/malformed native
admission before model discovery or launch. Neither command is proof that the
company CRM is authenticated. The installed binary/version was not reverified
during this lane; the predecessor recorded 0.155.0-alpha.16. Do not substitute an
old PATH binary, update global configuration or weaken permissions to get a pass.

## One operator-facing activation action — after engineering acceptance

The existing platform operator selects the exact admitted **venue-scoped MCP
credential** at `/admin/clients/<admitted-tenant-id>/credentials`, checks its
scope and `agent-runs:execute` capability, and confirms **Activate bridge
credential**. That existing owner performs the versioned activation and audit;
it does not start a runner, grant prospect scope or approve sending.

**No live tenant/venue/run/credential profile has been admitted in this lane.**
The placeholder above is not a discovered account. Before presenting that action
to Tom, the implementation owner must pin the private target, exact queued run and
reference-only credential identity through existing owners; keep the secret only
in the authorized process credential channel. Never paste it into a prompt, file,
receipt or URL. Session and lease references then come from native responses.
Because HTTP acceptance and private profile admission are still held, this is NOT
a claim that the current checkout is one click away from a working connection.

## No-send boundary

Configured sender: `tomschoenekase@torchiko.com`; actual company-account identity
remains unconfirmed. `deliveryEnabled:false`. Human send approval: absent.
Provider acceptance, provider Sent, receiving-inbox receipt and reply import:
NOT RUN. The synthetic invalid inbox is never a real receiving address.
Read `SELF-TEST.md` before any separately authorized later self-test. Never enable
the deliberately dark old loopback sales route as an authentication shortcut.
