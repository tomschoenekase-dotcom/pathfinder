# Torchiko Closeout B — final native HTTP proof, private authority still required

**The existing native authenticated transport and immutable writer roundtrip now pass. The installed private Codex → WLT/reference → CRM route remains held at an explicit target/authority admission, not at a missing implementation or a missing Codex login.** No new model run, email, send approval or deployment occurred.

## 1. Supported source and operating entry

Continue this existing checkout:
`C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-four-chat-lane-03-outreach\worktree`

Branch: `machine/torchiko/20260923-four-chat-lane-03-outreach`.
Frozen code commit: **ac33b42cc3a77bcbf8d53b7b6493d6332d12401a**.
Frozen code tree: **d20826815cc3da08d822b37c7f1604d2fdb7ef83**.
The later documentation-only checkout commit/tree and final clean state are recorded in `..\offer\r002\CHECKOUT-RECEIPT.json`. Runtime code must remain byte-identical to this tested checkpoint.

Current exact evidence and source mapping: `..\offer\r002\SOURCE-MANIFEST.json`.
Current narrow offer: `..\offer\r002\README.md`.
Current human gate: `..\offer\r002\PRIVATE-OPERATOR-GATE.md`.
The preserved r001 package and historical pre-closeout sections are not the current readiness state. Do not repeat their RAM-blocked preflights.

The user-level installed outreach skill still points to the preserved predecessor. It was **not** silently repointed. Tonight's authorized Codex entry must use this checkout's local `AGENTS.md`, `.agents/skills/torchiko-outreach/SKILL.md` and existing launcher. No additional chat was launched by B.

## 2. Executed final gates — kept separate

| Boundary                                                                      | Final result                              | Evidence and limit                                                                                                                                                                                                                              |
| ----------------------------------------------------------------------------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Final lifecycle, MCP, launcher, exact-retry and schema-admission Node cases   | **47/47 PASS**                            | Final exact CI; zero failed, skipped, cancelled or todo. Includes the previously held final18 lifecycle cases.                                                                                                                                  |
| Native registry, exact-run selection, actor/task/receipt and review DOM cases | **46/46 PASS**                            | Seven files, including8 review-component cases; zero pending/todo. Not authenticated browser or physical-device evidence.                                                                                                                       |
| Complete current-schema Prisma generation                                     | **PASS**                                  | All source schema tokens match the pinned native formatter's disposable copy and full generated client. No omitted model or borrowed compatible-client substitution.                                                                            |
| Full existing migration chain                                                 | **PASS**                                  | Fresh task-owned PostgreSQL, separate migration/runtime principals, unchanged254 migrations. Existing/shared/original databases were not modified.                                                                                              |
| Actual native HTTP read → task → import → reopen                              | **26/26 PASS**                            | Actual POST route, credential verification, native session/worker, selected queued run, lease and DB owners. Component input is expressly synthetic; not installed WLT or generated Codex prose.                                                |
| Post-commit lost response and exact recovery                                  | **PASS**                                  | Socket response lost after the real import committed; retained original candidate bytes recovered; two retries return the same receipt/draft; reopened exact subject/body remains version1 and DRAFT_REVIEW; total native draft count remains1. |
| Denial and lifecycle faults                                                   | **PASS**                                  | Disabled/missing/revoked authority; wrong tenant/run/lease; forged immutable binding; heartbeat response loss; cancellation; expired lease; held clients do not reconnect, select another run or write.                                         |
| No-side-effect boundary                                                       | **PASS**                                  | Zero provider/research requests, mailbox accounts, send items, outbox entries, email messages or approvals in the actual disposable acceptance. Container discarded.                                                                            |
| Installed Codex discovery/login                                               | **PASS, diagnostic only**                 | Existing0.155.0-alpha.16 binary and existing ChatGPT login; no global configuration or account change.                                                                                                                                          |
| Original installed WLT/reference bindings                                     | **PASS, local read-only diagnostic**      | Installed entrypoint matches pinned runtime; original runtime validator passes;203 component bindings hashed; exact supplied reference readable. No draft/model/HTTP/database action in this diagnostic.                                        |
| Actual installed Codex using legitimate private CRM scope                     | **NOT RUN — operator admission required** | No legitimate reference-only private endpoint/tenant/venue/credential/selected-run binding was available to B. No substitute credential or live run created.                                                                                    |
| Normal authenticated desktop/tablet/keyboard review                           | **NOT RUN**                               | Native review API and DOM tests pass; actual private UI session remains unproved. Desktop observe returned a real tool-not-found error; no browser state extraction was substituted.                                                            |
| Whole application semantic typecheck/build                                    | **NOT RUN BY B**                          | Focused source-export acceptance is not a whole B checkout build or A's product integration acceptance.                                                                                                                                         |
| Exact company mailbox / reply import                                          | **UNKNOWN / NOT RUN**                     | C's final company-account evidence is separate. Configured sender, DNS or generic Gmail readiness is not account/sync proof.                                                                                                                    |

Final CI export **cebd8fc28aafcc8ee8949c9be891a9c52a0d28c9**, tree **71c737586f3aacc7d49a659abd76540a26a77e2b**; existing repository run **35938134317**, job **107439743620**, conclusion **success**. Every explicitly selected input blob is checked against the operating code and CI tree. This public **source-only test export is NOT an integration patch for A** and contains no private corpus/reference, environment, credential or CRM records.

Complete original receipt bytes were reconstructed from bounded compressed chunks and SHA256-verified, not trusted from truncated annotations:

- HTTP receipt: `..\offer\r002\receipts\HTTP-ACCEPTANCE.json`, SHA256 **9cbd5eb92cee2566922ab253340c23aac0e5d5802eb1a74059064877c9cb43e6**.
- Resource/cleanup receipt: `..\offer\r002\receipts\RESOURCE-RECEIPT.json`, SHA256 **6d80959a4367e5d406055d56c49baa57f12a48eda7f2da1ca2f9f4a663a97e31**.

The real transport rate limit is unchanged:30 attempts/60seconds. The acceptance includes a single61second quiescent pause between already-settled fixtures; it never resets the limiter or retries an uncertain operation as a new request. Recorded HTTP timings are same-run fixture timings, not product/staging latency comparisons.

## 3. Actual remaining operator gate

**PRIVATE_NATIVE_TARGET_ADMISSION_REQUIRED.** The existing platform operator must identify/admit the private application target that actually contains this native writer/component source, and its exact tenant/venue, existing credential identity and separately authorized queued OUTREACH run. Preserve live and frozen native writer/read grants and the existing version/scope checks. The user does not manufacture session or lease IDs; native owners return them after admission.

The known loopback directory at127.0.0.1:58618 reports `EXISTING_OPT_IN_LOCAL_OPERATOR`, `authenticatedHuman:false` and `operationalDeploymentVerified:false`. It is not an admitted native CRM host. Do not turn on the old rehearsal/sales/research flags, extract a browser session, copy secrets into a prompt/file, claim an arbitrary run, or treat a Codex login as CRM authority.

Once the actual target is identified, the existing operator selects its exact venue MCP credential at `/admin/clients/<admitted-tenant-id>/credentials` and uses **Activate bridge credential** only when that credential is inactive and activation is needed. This is the existing versioned/scoped action, not permission to issue another credential, grant prospect scope or send mail. The route parameter above is deliberately not a fabricated account ID. An already-active credential needs no redundant activation.

After legitimate binding, the existing process-scoped launcher is `node scripts/torchiko-outreach-codex.mjs start`, retaining workspace-write/on-request approvals. B did not launch it. One separately authorized foreground run must still consume the actual installed WLT/reference, import and reopen an immutable review draft, then inspect the normal authenticated review UI. Until that is executed, do not mark the installed route accepted or repoint the global skill.

The installed reference hash is **235d91e6a575d8535566a3e316c1638d833128916cf0d926211c047384fbdf43**. Reuse that already-supplied reference and original WLT owner; zero Approved Language approvals is not an engineering blocker. The synthetic CI component fixture is never a private runtime fallback.

## 4. Minimal offer to A and independent checkout decision

`..\offer\r002\native-claim.patch` changes exactly four shared source/test files for optional exact-run selection. It needs **zero migrations**. Against A's observed **53d4366e63b022cfb9c30ba0b312282b6f917868**, private-index apply-check exited0, changed exactly those four paths, and reverse restored the exact original tree. A's worktree was not edited. Recheck A's current source and dirty state before any later application.

This patch alone is **not a full outreach installation**. `PREREQUISITES.json` separately enumerates the existing native writer/task/snapshot/import/review, component, schema/router and foreground-route files by exact B/A blobs. A lacks parts of that stack. No full registry/schema replacement, geography/county/research-lease migration, whole261-file archive or CI export is admitted. Keep A's product candidate and B's private operating checkout independent for tonight's aggregate rather than force an untested merge.

## 5. Preservation, cleanup and unfinished evidence

All **261/261** selected live predecessor files match the original frozen archive and manifest in the fresh read-only verification; zero drift. Original manifest/archive SHA256s are retained in `receipts/CLOSEOUT-B-PRESERVATION.json`. B made no original CRM/database mutation; the historical original-data fingerprint was not freshly recomputed. Earlier real Codex draft-v6 evidence is preserved but was not used as this HTTP acceptance.

Earlier CI failures remain in B's QA records: exact export-manifest/Clerk fixture setup, native schema formatting comparison, declared-workspace TSX resolution, real rate-limit pacing and receipt truncation. Those were repaired in the existing harness or CI configuration, not by changing native auth, lease, writer, retry, schema, migration or sending behavior.

Windows free RAM was below the unchanged5GiB reserve at entry. Existing hosted CI was invoked within14minutes; no repeated local RAM preflight, heavy local run, foreign process termination or shared lock claim. The first complete26-check native HTTP success arrived before the conservative45minute checkpoint. All owned disposable databases/containers were cleaned up; no Windows heavy slot or browser is reserved by B.

No production/staging deployment, company-mailbox substitution, provider/model call, outreach research, approval or live email occurred. A alone owns staging decisions; C owns mailbox evidence. Neither a green synthetic native proof nor midnight authorizes production or sending.
