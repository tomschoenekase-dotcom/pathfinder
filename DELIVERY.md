# Current Closeout B delivery

The final2026-09-24UTC checkpoint is `CLOSEOUT-B-DELIVERY.md`; current exact offer/receipt set is `..\offer\r002`. Native authenticated HTTP26/26, Node47/47 and native/review46/46 now pass, together with full current schema generation and unchanged migrations. Actual installed private authority/model/UI and company-mailbox gates remain unproved. No send, approval, deployment, new paid call, global skill change or original-data mutation occurred.

The prior delivery below is retained as historical evidence and is superseded where the current closeout explicitly updates a test result.

## Archived r001 — source implementation, acceptance held at that checkpoint

**Not an accepted authenticated connection.** This delivers isolated source and
concrete tests/harness, preserving the predecessor's real Codex/native draft and
exact recovery. Actual native HTTP acceptance did not finish; this limitation is
not replaced by a local auth-hold, mocked transport or older SQL proof.

## Implemented source

The existing MCP adapter now manages a foreground native session/worker and the
lease of exactly the selected already-authorized run. It does not create a CRM,
writer, outbox, credential owner, run scheduler or mailbox adapter. Native claim
accepts optional exact-run narrowing, preserving the previous default behavior
for existing runners. Frozen/live grants remain native authority.

Heartbeat, expiration, cancellation, wrong selected run and missing authority
stop the operation. Exact imported bytes remain in the existing retry store.
Successful settlement requires a confirmed immutable review receipt; subsequent
uncertain import cannot settle using an older receipt. The launcher rejects a
missing native connection before discovering/starting a model. No global Codex
installation, account, model, approval or sandbox configuration was changed.

The existing native review component now explicitly reports an unconfirmed
receipt while keeping the exact candidate, displays its bound recipient without
guessing, and handles long text without forcing horizontal overflow. These are
source changes, not a claimed authenticated browser pass. UI tests are present;
the final DOM/browser runs remain held.

The native HTTP acceptance script uses the actual dashboard route, credential
issue/activation/verification/revocation, session/worker/lease and native writer
owners. Its synthetic source fixture is explicitly hand-authored contract data,
not public research or a genuine captured venue. It is designed to exercise
post-commit socket loss and exact retry. No successful receipt is claimed because
the executed attempts stopped in setup/resource admission.

## Provenance and handoff

Source: `C:\Users\tomsc\MachineWorkspaces\torchiko\20260923-four-chat-lane-03-outreach\worktree`.
Exact final commit/tree, changed paths, file hashes and patch roundtrip live in
`..\offer\r001\SOURCE-MANIFEST.json`. The narrow patch is `..\offer\r001\source.patch`.
See `INTEGRATION-OFFER.md` and `INTEGRATION-REQUESTS.md`; Lane 4 owns admission.

Preservation snapshot: `fe205d86961186225fed24836ed155a941acada9` /
`2ef4a76be03a218f95527d6162b12994cb34e6a5`, based on `8610d0d48cd8e6fdaff40c74421f5f491f8fe119`.
All 261 archive-selected files were verified against live predecessor source.
Archive SHA256: `c9591cf0e63603e274039a05e76de4bfde4a256fe696e80deb2923878f8ef874`.
Original manifest SHA256: `d9cb108b958df384d922c5a2e566953c11d0b6aab6399f36b910589c4275951f`.
Original fingerprint: `c1e7fb4481d1a1a95862da10920e214af34edd6450e2a48449c44a405ce19b7b`.
The archive/manifest remain unchanged in own QA. Neither is blanket integration
authorization; no original dirty CRM source or retained business database changed.

## Validation and remaining gates

`AUTHENTICATED-OUTREACH-ACCEPTANCE.md` is the explicit PASS/FAIL/HELD/NOT RUN matrix.
The accepted historical Node log shows 16/16 passing, zero skipped. Two final
additional cases and final source changes need the final rerun. The older wrapper
reported a null exit code despite the complete passing test log; that reporting
bug was fixed, not hidden. Broader native/registry/UI tests, typecheck, build and
actual browser inspection are not represented as passing.

Native attempt 1 reached the inherited guest-disposition migration, then failed
under an application database role that cannot perform its cluster-role setup.
The new empty database and role were discarded; no migration was weakened.
Attempt 2 used a separately owned PostgreSQL container/migration principal and
was stopped at 5.049 GiB free by the shared memory guard; the owned container was
removed. Subsequent free RAM was below the required five-GiB reserve. No unowned
process was stopped, and no own container/service is left running.

Full private Prisma generation was separately resource-aborted at 5.022 GiB.
For bounded native acceptance only, 503 model/enum definitions were compared and
found identical; 136 frozen private client files were hash-verified. Three
unrelated county/intake models are explicitly excluded/denied. This limited client
admission is NOT a full current-schema build or geography acceptance.

Actual HTTP first, then same-authority installed Codex, then normal authenticated
desktop/tablet/keyboard review remain the next engineering gates. The precise
existing operator activation action and the still-unadmitted private references
are documented in `CODEX-START-HERE.md`; it does not pretend credentials are the
only unfinished step. No new worker chats were launched.

## Release and mailbox states

Production/staging mutations: none. Production authorized: false. Send authorized:
false. No email, send approval, personal-mailbox substitution, market research,
customer publication, OAuth/grant widening or provider spend occurred.
Company mailbox identity remains unconfirmed. Earlier real Codex draft-v6 and
native exact-recovery evidence was preserved, not rerun or relabeled as new
authenticated HTTP proof. No current installed-Codex model call was made.
