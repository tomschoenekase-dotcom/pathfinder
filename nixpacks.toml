[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @pathfinder/dashboard build"]

[start]
cmd = "node apps/dashboard/.next/standalone/server.js"
