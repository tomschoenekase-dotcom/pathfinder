---
name: torchiko-outreach
description: Use for Tom's Torchiko venue outreach: drafting an individual email, preparing or reviewing a group of up to 50 Chicago/Evanston venues, asking for 50 more, and reviewing company-mailbox replies. Do not use for generic writing, other projects, visitor/product development, or unrelated questions. Preparation is never approval or permission to send.
---

# Torchiko connected outreach

## Find the actual owner first

This skill belongs to the isolated Lane 3 implementation checkout at
`C:/Users/tomsc/MachineWorkspaces/torchiko/20260923-four-chat-lane-03-outreach/worktree`.
Its native HTTP acceptance is HELD; do not advertise a completed connection.
The earlier installed user-level skill still refers to the preserved predecessor;
it has not been repointed. In this checkout this local skill and its current
CODEX-START-HERE are authoritative. Do not silently switch to predecessor source.
Read `CODEX-START-HERE.md` there and this skill's `runtime-profile.json`. Do not
edit the old CRM checkout, the staging-polish owner's files, production or global
Codex configuration. From a wrong directory, use the absolute owner path for
these commands; do not turn the other project into a Torchiko workspace.

For an outreach request, run the read-only diagnostics:
`node scripts/torchiko-outreach.mjs doctor` and
`node scripts/torchiko-outreach-codex.mjs doctor` from the owner checkout.
The old PATH binary is not the verified operating route. The wrapper locates the
compatible already-installed Codex; it never installs or upgrades it.

## Discover tools, not imagined access

Prefer actual authenticated native `torchiko.prospects.*` tools exposed through
the existing execution bridge. A tool schema or ChatGPT login is not a live CRM
grant. The native run must have an active credential/session/lease, complete
territory scope, `prospects.read`, `prospects.correspondence.read`, and
`prospects.native-writer`; cohort maintenance also needs `prospects.maintain`.
Never manufacture an actor, credential, scope or lease in tool arguments.

Launching through `torchiko-outreach-codex.mjs start` adds a process-scoped
`torchiko_outreach` MCP adapter. Start with its local `torchiko_outreach_status`:
missing configuration is an honest hold, not a request to mint credentials. The
adapter routes selected tools through the existing bridge `callProspectTool` method.
It never grants access or creates a native run. With a legitimately admitted
credential, exact target and selected run, it registers its own foreground session
and worker, claims ONLY that run, and heartbeats the existing native owners.
Session/lease IDs are native responses, not operator inputs. Legacy attached
references remain supported only when their existing owner maintains the lease.
Missing authority blocks launcher start before model discovery. Server rollout,
credential activation and frozen/live grants remain separately required.
Do not mistake the generic `/api/mcp/` operational registry for prospect authority.

Use `torchiko_outreach_compile` with `{task,candidate,modelIdentity}` to compile
your original text and retain exact bytes without a shell. It returns `result`
for the native import tool plus a local artifact reference, not an import receipt.
`torchiko_outreach_recover_result` takes `{taskId}` or `{taskId,sha256}` to locate
or read one original result. Local recovery is not proof of live native freshness.

When those tools are not exposed, the opt-in local no-send operator CLI is an
explicitly different route, fixed to loopback port 58618. It does not authenticate
the business agent or mailbox. A read-only directory can be running while its
sales route is disabled. Report `LOCAL_SALES_DISABLED_OR_UNAVAILABLE` after that
actual failure, retain the request, and stop that write. Do not restart another
owner's service, change flags, query the database directly or remove a guard.

## One individual email, from native evidence to a native review revision

1. Resolve the real native location with `search_venues` / `read_venue`, not an
   organization name guess or a capped first page. Respect canonical IDs and
   geography holds. Inspect its current draft, suppression and complete retained
   correspondence via `read_outreach_review` / `read_native_writer_task`.
   On the local route use `node scripts/torchiko-outreach.mjs inspect --venue-id ID`.
2. Reuse current durable native evidence. This Lane 3 assignment authorizes NO
   market research or additional worker chats. In a separately authorized future
   outreach task, research only a missing/stale fact that
   improves THIS email. Preserve useful facts with source URL, date, exact venue
   identity and uncertainty through existing `append_venue_evidence`, native
   claim admission and research tools. Do not store whole crawls, infer contact
   permission, treat supplied references as verified product facts, or resolve a
   physical county from an old worksheet/HQ label. Missing history is not no history.
3. Select `torchiko-v0.2` using the actual current named-guide
   identity returned by native readiness and its exact hash (the contract's enum
   is authoritative; do not guess it). Call `prepare_native_writer`, then
   `read_native_writer_task`. Reuse a current task instead of rebuilding its
   binding. Local `prepare --venue-id ID` reads the supplied private reference.
4. Read the task's actual installed WLT packet, selected reference, venue facts,
   claims, prior drafts, current reply context and constraints. Do not rebuild
   WLT or ask Tom to supply the reference again. Zero approved phrases is fine;
   no approved phrase may be invented or copied from acceptance fixtures.
5. YOU, the current actual model, write a short original venue-specific subject
   and email. No template-substitution script. Use only supported claims; distinguish
   proposals from current capability. No invented visit, price, approval or send.
   The reference informs tone; it does not require a quota of visitor questions.
6. Produce `torchiko.codex-outreach-text/1` fragments from
   `node scripts/torchiko-outreach.mjs schema`: every fragment has exact original
   `text`, honest `category`, supported `claimIds`, substantive `reason`, `answers`.
   Include your own blank lines. `compile --stdin` accepts
   `{task,candidate,modelIdentity}` and mechanically computes Unicode claim spans.
   It does not rewrite your text, evaluate it as human, or authorize sending.
   Use the ACTUAL model identity; do not label a fixture or hand template as Codex.
7. `compile --stdin`, local `import --stdin` and the MCP native import adapter
   automatically retain the exact bounded result in this checkout's ignored
   `.outreach-retry/<native-task-id>/<sha256>.json` before import. Use `retain --stdin`
   for an already compiled native result. This is a retry payload, not a CRM ledger.
   Import with `import_native_writer_result` or local `import --stdin`. Preserve
   task binding and expected native hashes. Do not use old generic
   `save_outreach_draft` as a substitute for the native writer roundtrip.
8. If the response is lost, retry the SAME exact result before any fresh read,
   task regeneration or preparation. The immutable native receipt owns recovery.
   Reopen the saved draft/version and show exact subject/body, recipient, current
   holds and review state. A receipt replay proves old persistence, not freshness.
   In a fresh chat, use `recover --task-id TASK` then
   `read-result --task-id TASK --sha256 HASH` to recover unchanged bytes. Local
   `retry --task-id TASK --sha256 HASH` uses the guarded local operator route;
   authenticated tools must instead reuse the same native import arguments. Do
   not switch transports, select a newer variant or re-prepare to bypass a hold.

## Up to 50; then 50 more

Resolve an explicit bounded native candidate list; no live bulk research in this
acceptance session. `preview_outreach_cohort` takes exact venue/contact IDs,
`excludePriorGroups:true` and count at most 50. Unknown size/contact/history or
unreviewed geography are explicit holds; large/strategic, suppression, existing
drafts/relationships, same organization or duplicate mailbox and ANY prior group
are excluded. Never invent records to hit 50 or overwrite an earlier group.

After reviewing the exact preview, `reserve_outreach_cohort` pins it on native
campaign/members with an unchanged request key and preview hash. Claim at most
five using `claim_outreach_window`; reuse its same request key after a lost
response. For each member use the individual native path above, checkpoint the
exact task and then the actual import receipt via `checkpoint_outreach_member`.
Stop only held records and continue independent eligible records. A task left by
an expired lease must be recovered, not automatically regenerated. No local JSON
"campaign database" and no bulk send.

Reopen previous groups with `list_outreach_cohorts` and follow its cursor. An invalid
cursor is an error, not an empty directory. The complete read includes this actor's
own `recoverableWork` lease/task references. Explicit `release` is supported on the
member checkpoint; releasing a task with uncertain import requires receipt recovery.
Only an authenticated HUMAN can pause/cancel/resume the entire group in the admin
page. These operations never discard drafts or make prior venues eligible for 50 more.

`read_outreach_cohort` returns EVERY selected row, exact latest text/version/hash,
recipient, stale/held states and native correspondence evidence. The normal admin
review page is `/admin/prospects/preparation`; open the retained cohort ID. The
existing `/admin/prospects/outreach` center and its feature gate are preserved. Human
"read acknowledgement" is NOT meaning approval or send approval. To get 50 more,
make a new explicit preview; the native prior-group exclusion persists through
fresh chats and unfinished earlier work.

## Reply and delivery boundaries

Sender is only `tomschoenekase@torchiko.com`. Never substitute personal Gmail.
Use current native outbox/provider/correspondence owners and retained message
coverage. Unknown transport outcome means reconcile the existing item, not a new
send. Provider acceptance, provider Sent, recipient receipt and incoming reply
ingestion are distinct states. Expired or ambiguous reply bodies are held; select
the exact thread and recover source coverage before writing a reply.

Read `SELF-TEST.md` before a later live self-test. No live send is authorized now.
One confirmed Tom-owned receiving inbox, the exact recipient+message approval,
company-account authentication, delivery/release authority and no-send/release
gates must all be established separately. Never approve, enable delivery, invite,
publish, merge master, deploy, or buy anything on a preparation request.
