# Closeout B requests — CURRENT

Current bounded decisions are in `..\offer\r002\PREREQUISITES.json` and `PRIVATE-OPERATOR-GATE.md`. A may review the four-file exact-run patch independently; it needs zero migrations and is not a full writer installation. Keep A's product source and B's private operating checkout independent for tonight's aggregate. Do not merge the CI export or261-file predecessor.

The old HTTP/full-schema/migration/final-test holds below are resolved by final run35938134317 (47Node/46native-review/26actualHTTP PASS). Installed private target/authority and authenticated model/UI proof are **not** resolved. B did not create a live credential/run or activate a target. Existing active credentials must not be redundantly activated. Full details: `CLOSEOUT-B-DELIVERY.md`.

## Archived r001 requests — preserve history, do not repeat superseded holds

## L3-01 — Native exact-run claim narrowing (new shared-owner change)

Review `native-claim.patch` only: optional runId schema forwarding and exact native
query predicate, with a defensive no-fallback assertion. The foreground writer
must never claim another queued run when its reviewed run cannot be claimed.
No migration/config/contract-wide capability expansion is required by this new
field. Preserve newer helper/registry logic and rerun both old and new claim tests.
Status: SOURCE OFFER; final native/registry tests and actual HTTP acceptance held.

## L3-02 — Existing writer/credential/component prerequisites (inherited)

The 261-file snapshot carries the native writer API/service, DB-issued actor,
immutable snapshot/task/import/review owners, generated tool contracts, strict
credential/run/lease ownership, and private installed WLT/reference component
route. Reconcile those exact inherited files individually against current product
source; do not copy the whole API/router/config tree. Root/base config, SQL boundary
inventories and worker files are not owned for wholesale admission by this lane.

The private component flag `TORCHIKO_AUTHENTICATED_CRM_SALES_ENABLED=1` and existing
`AGENT_BRIDGE_HTTP_ENABLED` must be admitted only to an explicitly reviewed private
target. No hosted flags were changed here. The old local rehearsal/sales/research
flags must remain off for authenticated native writer proof; never turn on the
old loopback route to get around business-agent authentication.

## L3-03 — Inherited migration decisions are separate

`20260922180000_chicago_venue_intelligence` supplies inherited venue intelligence
storage; it is not a new requirement introduced by optional exact-run claiming.
Reconcile whether the selected native writer/read tools need its fields on the
actual candidate before admitting the existing migration.

`20260922234500_prospect_county_territories` supplies inherited authoritative
physical county/territory definitions and related reads. It does not authorize
new research or deriving county from an old worksheet label. Coordinate complete
territory/grant ownership independently from the one-run foreground lifecycle.

`20260923014500_prospect_county_research_leases` supplies inherited research lease
ownership. This assignment performs no territorial research and its bounded
native client explicitly excludes that model. Do not claim this migration tested
or required for the narrow authentication patch simply because it is archived.

All three remain NOT ADMITTED to staging by Lane 3. The synthetic full migration
chain was not skipped/weakened. Full candidate generation/migration compatibility
remains an integration gate; the compatible 503-block private client is not a
substitute for full generation or those migrations' acceptance.

## L3-04 — Exact private connection admission, then one operator activation

After provider-dark HTTP acceptance passes, bind a reference-only private target,
native tenant/venue, exact queued run and credential identity using the existing
platform/credential/run owners. No such live profile was legitimately available
or created in this lane; do not invent identifiers or stash secrets in a profile.
Keep the actual secret in the authorized process credential channel only.

Then the sole operator-facing activation is to select that exact venue MCP
credential in `/admin/clients/<admitted-tenant-id>/credentials` and confirm
**Activate bridge credential**, with its existing scope confirmation and version
guard. It must carry `agent-runs:execute`; the existing native identity/run still
must independently have scoped writer/read grants. Session and lease references
are now returned/renewed by native owners, not manually entered.

This is conditional, not a claim that missing credentials are the only unfinished
gate. Native HTTP, installed Codex and authenticated desktop/tablet/keyboard review
must still be proved, under the shared memory reserve, before any source promotion.

## L3-05 — Preserve other lane ownership and newer source

No visitor shell, ChatWindow, VenueChatExperience, deployment, worker startup,
release identity or hosted runtime source was edited by this new lane. Lane 4's
latest status is authoritative for those. Reconcile its current HEAD/dirty state;
do not reset it to the older preservation base or copy old schema/worker files over
newer diagnostics. Installed global skill repointing and any later live mailbox
connection are explicit later admissions, not side effects of this source offer.
